abstract class Player {
    public x: number;
    public y: number;
    public choice: number;

    public constructor(x: number, y: number, choice: number) {
        this.x = x;
        this.y = y;
        this.choice = choice;
    }
}

